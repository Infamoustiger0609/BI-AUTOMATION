apiVersion: v1
kind: Service
metadata:
  name: prompt2pbi-redis
  namespace: prompt2pbi
spec:
  selector:
    app: prompt2pbi-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP

